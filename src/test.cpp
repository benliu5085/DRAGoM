#include <iostream>
#include <fstream>
#include <boost/tokenizer.hpp>
#include <string>

std::string CPU;
std::string DIR_CDHIT;
int main(int argc, char* argv[])
{
    std::string header = "xxx";
    int cnt_tails = 0;
    header += std::to_string(cnt_tails);
    std::cout << header << "\n";

   
    return 0;
}